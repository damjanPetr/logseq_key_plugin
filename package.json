{
  "name": "logseq_key_plugin",
  "version": "1.0.0",
  "description": "a logseq plugin that would eneable going to a specific page with on the click of a button",
  "main": "src/index.js",
  "logseq": {
    "id": "_5pm5r11fs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --outDir ./dist",
    "start": "node dist/index.js",
    "watch": "tsx --watch src/index.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "@logseq/libs": "^0.0.17"
  }
}
